
from setuptools import setup, find_packages

setup(
    name='geturlparse', 
	version='1.0.0', 
	author='changwoo', 
	author_email = 'changwoo360@gmail.com',
    packages = find_packages(),
    include_package_data = True,
    platforms = 'any',
    install_requires = [],
)

